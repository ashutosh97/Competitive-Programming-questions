#include <stdio.h>

int main(void){
  int a, n, b=0, i;

  scanf("%d %d", &a, &n);
  while(n<=0){
    scanf("%d", &n);
  }
  for(i=1; i<=n;i++){
    b+=a;
    a++;
  }

  printf("%d\n", b);

return 0;
}
