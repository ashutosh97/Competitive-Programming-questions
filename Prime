#include <stdio.h>
#include <math.h>

int main(void) {
  int num, raiz, contador, i;
	i=0;
	contador=1;
	scanf("%d", &num);
	raiz = sqrt(num);
	while(contador<=raiz){
	  if(num%contador==0){
    	i=i+1;
  }
	  contador=contador+1;
  }
	if(i==1){
    printf("Is prime\n");
  }
	else
	printf("Not is prime\n");
  return 0;
}
